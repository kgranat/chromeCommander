{
  // This is required for every app
  "manifest_version": 2,
  "name": "Chrome Commander",
  "version": "1.0.0",
  "app": {
    "background": {
      "scripts": ["background.js"]
    }
  },

  // Needed to get to the serial ports
  "permissions": ["serial"]
}
